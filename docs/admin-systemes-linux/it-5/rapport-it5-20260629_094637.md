# Rapport automatique - Iteration 5 AlpesNet

| Champ | Valeur |
| --- | --- |
| Date | 2026-06-29 09:46:37 |
| Machine | srv-oliv |
| Source | /srv/alpesnet |
| Destination rsync | /backup/alpesnet-20260629 |
| Archive configs | /backup/configs-20260629.tar.gz |
| Restauration test | /tmp/restauration-test |
| Hote intranet | intranet.alpesnet.local |
| Utilisateur Nginx | www-nginx |
| Sous-reseau SSH | 192.168.56.0/24 |
| Mode dry-run | 0 |

Ce rapport liste les commandes executees par le script, leur resultat, leur explication, la timeline, les causes/risques traites, les actions correctives et l'etat final verifiable.


# Preparation

## Preparer le dossier de sauvegarde

**Commande**

```bash
mkdir -p '/backup' && chmod 750 '/backup' && ls -ld '/backup'
```

**Resultat** : code retour `0`

```text
drwxr-x--- 6 root root 4096 29 juin  09:17 /backup
```

**Explication**

Cree le dossier de sauvegarde avec des droits restrictifs.

## Verifier ou creer la source AlpesNet

**Commande**

```bash
mkdir -p '/srv/alpesnet/logs' '/srv/alpesnet/projects' '/srv/alpesnet/secrets' '/srv/alpesnet/web'; [ -f '/srv/alpesnet/preuve.txt' ] || printf '%s\n' 'preuve sauvegarde AlpesNet' > '/srv/alpesnet/preuve.txt'; [ -f '/srv/alpesnet/logs/system.log' ] || printf '%s\n' 'log test AlpesNet' > '/srv/alpesnet/logs/system.log'; [ -f '/srv/alpesnet/projects/projet-alpha.txt' ] || touch '/srv/alpesnet/projects/projet-alpha.txt'; [ -f '/srv/alpesnet/projects/projet-beta.txt' ] || touch '/srv/alpesnet/projects/projet-beta.txt'; [ -f '/srv/alpesnet/secrets/db.conf' ] || printf '%s\n' 'db=alpesnet' > '/srv/alpesnet/secrets/db.conf'; find '/srv/alpesnet' -maxdepth 2 -type f -ls
```

**Resultat** : code retour `0`

```text
   917519      4 -rw-r--r--   1 root     root           27 juin 26 13:46 /srv/alpesnet/preuve.txt
   917521      0 -rw-r--r--   1 root     root            0 juin 26 14:06 /srv/alpesnet/projects/projet-alpha.txt
   917522      0 -rw-r--r--   1 root     root            0 juin 26 14:06 /srv/alpesnet/projects/projet-beta.txt
   917517      4 -rw-r--r--   1 root     root           45 juin 23 14:30 /srv/alpesnet/logs/system.log
   917515      0 -rw-r--r--   1 root     root            0 juin 23 14:30 /srv/alpesnet/projets/projet-alpha.txt
   917516      0 -rw-r--r--   1 root     root            0 juin 23 14:30 /srv/alpesnet/projets/projet-beta.txt
   917518      0 -rw-------   1 root     root            0 juin 23 11:31 /srv/alpesnet/secrets/test-secret.txt
   917514      4 -rw-r--r--   1 root     root           30 juin 23 14:30 /srv/alpesnet/secrets/db.conf
```

**Explication**

Prepare une source de test si le dossier /srv/alpesnet est vide, puis liste les fichiers a sauvegarder.


# Exercice 1 - Sauvegarde rsync

## Executer la sauvegarde rsync

**Commande**

```bash
rsync -avz --delete '/srv/alpesnet/' '/backup/alpesnet-20260629/'
```

**Resultat** : code retour `0`

```text
sending incremental file list

sent 495 bytes  received 17 bytes  1.024,00 bytes/sec
total size is 102  speedup is 0,20
```

**Explication**

Synchronise la source vers un dossier date. L'option --delete garde la destination conforme a la source.

## Verifier le contenu sauvegarde

**Commande**

```bash
find '/backup/alpesnet-20260629' -maxdepth 2 -type f -ls
```

**Resultat** : code retour `0`

```text
   131116      4 -rw-r--r--   1 root     root           27 juin 26 13:46 /backup/alpesnet-20260629/preuve.txt
   131118      0 -rw-r--r--   1 root     root            0 juin 26 14:06 /backup/alpesnet-20260629/projects/projet-alpha.txt
   131119      0 -rw-r--r--   1 root     root            0 juin 26 14:06 /backup/alpesnet-20260629/projects/projet-beta.txt
   131117      4 -rw-r--r--   1 root     root           45 juin 23 14:30 /backup/alpesnet-20260629/logs/system.log
   131120      0 -rw-r--r--   1 root     root            0 juin 23 14:30 /backup/alpesnet-20260629/projets/projet-alpha.txt
   131121      0 -rw-r--r--   1 root     root            0 juin 23 14:30 /backup/alpesnet-20260629/projets/projet-beta.txt
   131123      0 -rw-------   1 root     root            0 juin 23 11:31 /backup/alpesnet-20260629/secrets/test-secret.txt
   131122      4 -rw-r--r--   1 root     root           30 juin 23 14:30 /backup/alpesnet-20260629/secrets/db.conf
```

**Explication**

Prouve que les fichiers attendus existent dans la sauvegarde rsync.

## Verifier rsync en dry-run checksum

**Commande**

```bash
rsync -avz --dry-run --checksum '/srv/alpesnet/' '/backup/alpesnet-20260629/'
```

**Resultat** : code retour `0`

```text
sending incremental file list

sent 623 bytes  received 17 bytes  1.280,00 bytes/sec
total size is 102  speedup is 0,16 (DRY RUN)
```

**Explication**

Compare le contenu source/destination sans modifier les fichiers. Une absence d'ecart significatif valide la synchronisation.


# Exercice 1 - Archive tar et restauration

## Creer l'archive des configurations

**Commande**

```bash
tar -czf '/backup/configs-20260629.tar.gz' /etc/ssh /etc/rsyslog.d && ls -lh '/backup/configs-20260629.tar.gz'
```

**Resultat** : code retour `0`

```text
tar: Suppression de « / » au début des noms des membres
tar: Suppression de « / » au début des liens physiques
-rw-r--r-- 1 root root 24K 29 juin  09:46 /backup/configs-20260629.tar.gz
```

**Explication**

Archive les configurations critiques SSH et rsyslog dans un fichier compresse.

## Lister le contenu de l'archive

**Commande**

```bash
tar -tzf '/backup/configs-20260629.tar.gz' | grep -E 'etc/ssh|etc/rsyslog.d'
```

**Resultat** : code retour `0`

```text
etc/ssh/
etc/ssh/sshd_config.bak.20260626_120855
etc/ssh/sshd_config.bak.20260626_120637
etc/ssh/ssh_host_rsa_key
etc/ssh/sshd_config
etc/ssh/ssh_config
etc/ssh/ssh_host_ed25519_key
etc/ssh/sshd_config.bak.20260626_113118
etc/ssh/sshd_config.bak
etc/ssh/ssh_host_rsa_key.pub
etc/ssh/ssh_host_ed25519_key.pub
etc/ssh/ssh_host_ecdsa_key.pub
etc/ssh/sshd_config.d/
etc/ssh/sshd_config.d/99-alpesnet-hardening.conf
etc/ssh/sshd_config.bak.20260626_112936
etc/ssh/ssh_host_ecdsa_key
etc/ssh/ssh_config.d/
etc/ssh/moduli
etc/rsyslog.d/
etc/rsyslog.d/50-alpesnet-ssh.conf
etc/rsyslog.d/20-ufw.conf
```

**Explication**

Controle le contenu de l'archive sans extraire les fichiers.

## Tester la restauration dans un repertoire temporaire

**Commande**

```bash
rm -rf '/tmp/restauration-test'; mkdir -p '/tmp/restauration-test'; tar -xzf '/backup/configs-20260629.tar.gz' -C '/tmp/restauration-test'; ls '/tmp/restauration-test/etc/ssh'; ls '/tmp/restauration-test/etc/rsyslog.d'; sed -n '1,20p' '/tmp/restauration-test/etc/ssh/sshd_config'
```

**Resultat** : code retour `0`

```text
moduli
ssh_config
ssh_config.d
sshd_config
sshd_config.bak
sshd_config.bak.20260626_112936
sshd_config.bak.20260626_113118
sshd_config.bak.20260626_120637
sshd_config.bak.20260626_120855
sshd_config.d
ssh_host_ecdsa_key
ssh_host_ecdsa_key.pub
ssh_host_ed25519_key
ssh_host_ed25519_key.pub
ssh_host_rsa_key
ssh_host_rsa_key.pub
20-ufw.conf
50-alpesnet-ssh.conf

# This is the sshd server system-wide configuration file.  See
# sshd_config(5) for more information.

# This sshd was compiled with PATH=/usr/local/bin:/usr/bin:/bin:/usr/games

# The strategy used for options in the default sshd_config shipped with
# OpenSSH is to specify options with their default value where
# possible, but leave them commented.  Uncommented options override the
# default value.

Include /etc/ssh/sshd_config.d/*.conf

#Port 22
#AddressFamily any
#ListenAddress 0.0.0.0
#ListenAddress ::

#HostKey /etc/ssh/ssh_host_rsa_key
#HostKey /etc/ssh/ssh_host_ecdsa_key
```

**Explication**

Extrait l'archive dans un dossier de test et verifie que les fichiers restaures sont lisibles.

## Generer et verifier le checksum

**Commande**

```bash
sha256sum '/backup/configs-20260629.tar.gz' > '/backup/checksums.txt'; cd / && sha256sum -c '/backup/checksums.txt'
```

**Resultat** : code retour `0`

```text
/backup/configs-20260629.tar.gz: Réussi
```

**Explication**

Genere une empreinte SHA-256 et verifie l'integrite de l'archive.


# Procedure de restauration

## Generer la procedure de restauration SSH

**Commande**

```bash
printf '%s\n' 'Procedure de restauration /etc/ssh' '1. Identifier la derniere archive valide dans /backup.' '2. Verifier son integrite avec sha256sum -c /backup/checksums.txt.' '3. Extraire l archive dans /tmp/restauration-test.' '4. Comparer les fichiers restaures avec les fichiers actuels.' '5. Copier uniquement les fichiers necessaires vers /etc/ssh.' '6. Verifier la syntaxe avec sudo sshd -t.' '7. Recharger SSH avec sudo systemctl reload sshd.' '8. Tester une nouvelle connexion avant de fermer la session active.' > '/backup/procedure-restauration-ssh-20260629.txt'; cat '/backup/procedure-restauration-ssh-20260629.txt'
```

**Resultat** : code retour `0`

```text
Procedure de restauration /etc/ssh
1. Identifier la derniere archive valide dans /backup.
2. Verifier son integrite avec sha256sum -c /backup/checksums.txt.
3. Extraire l archive dans /tmp/restauration-test.
4. Comparer les fichiers restaures avec les fichiers actuels.
5. Copier uniquement les fichiers necessaires vers /etc/ssh.
6. Verifier la syntaxe avec sudo sshd -t.
7. Recharger SSH avec sudo systemctl reload sshd.
8. Tester une nouvelle connexion avant de fermer la session active.
```

**Explication**

Produit une procedure courte a conserver dans le carnet de bord.


# Autonomie 3 - Deploiement Nginx securise

## Installer Nginx et les outils de securite

**Commande**

```bash
apt-get update && apt-get install -y nginx ufw fail2ban logrotate curl
```

**Resultat** : code retour `0`

```text
Atteint :1 http://deb.debian.org/debian bookworm InRelease
Atteint :2 http://security.debian.org/debian-security bookworm-security InRelease
Atteint :3 http://deb.debian.org/debian bookworm-updates InRelease
Lecture des listes de paquets…
Lecture des listes de paquets…
Construction de l'arbre des dépendances…
Lecture des informations d'état…
nginx est déjà la version la plus récente (1.22.1-9+deb12u8).
ufw est déjà la version la plus récente (0.36.2-1).
fail2ban est déjà la version la plus récente (1.0.2-2).
logrotate est déjà la version la plus récente (3.21.0-1).
curl est déjà la version la plus récente (7.88.1-10+deb12u14).
0 mis à jour, 0 nouvellement installés, 0 à enlever et 1 non mis à jour.
```

**Explication**

Installe le serveur web et les outils requis pour firewall, bannissement SSH, rotation de logs et tests HTTP.

## Creer l'utilisateur dedie Nginx

**Commande**

```bash
id 'www-nginx' >/dev/null 2>&1 || useradd --system --no-create-home --shell /usr/sbin/nologin 'www-nginx'; getent passwd 'www-nginx'
```

**Resultat** : code retour `0`

```text
www-nginx:x:999:996:Service Nginx AlpesNet:/nonexistent:/usr/sbin/nologin
```

**Explication**

Cree un compte systeme dedie sans shell interactif et sans home. Le worker Nginx tournera sous ce compte.

## Configurer la directive user de Nginx

**Commande**

```bash
cp -a /etc/nginx/nginx.conf /etc/nginx/nginx.conf.bak.20260629_094637; if grep -q '^user ' /etc/nginx/nginx.conf; then sed -i 's/^user .*/user www-nginx;/' /etc/nginx/nginx.conf; else sed -i '1i user www-nginx;' /etc/nginx/nginx.conf; fi; grep '^user ' /etc/nginx/nginx.conf
```

**Resultat** : code retour `0`

```text
user www-nginx;
```

**Explication**

Force Nginx a lancer ses workers avec l'utilisateur dedie www-nginx au lieu de www-data.

## Creer la racine web et la page intranet

**Commande**

```bash
mkdir -p '/var/www/intranet.alpesnet.local/html'; cat > '/var/www/intranet.alpesnet.local/html/index.html' <<HTML
<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Intranet AlpesNet - Oliv</title>
  <style>
    :root {
      color-scheme: dark;
      --bg: #09111f;
      --panel: #101b2e;
      --panel-2: #142238;
      --text: #eef6ff;
      --muted: #9fb2c8;
      --line: rgba(255,255,255,.12);
      --accent: #28d4b7;
      --accent-2: #5aa9ff;
      --warn: #ffd166;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      min-height: 100vh;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background:
        radial-gradient(circle at 15% 15%, rgba(40,212,183,.18), transparent 30%),
        radial-gradient(circle at 85% 8%, rgba(90,169,255,.18), transparent 28%),
        linear-gradient(135deg, #07101d 0%, var(--bg) 48%, #0f1a2d 100%);
      color: var(--text);
    }
    main {
      width: min(1120px, calc(100% - 32px));
      margin: 0 auto;
      padding: 42px 0;
    }
    header {
      display: flex;
      justify-content: space-between;
      gap: 24px;
      align-items: flex-start;
      padding-bottom: 30px;
      border-bottom: 1px solid var(--line);
    }
    .brand {
      display: flex;
      align-items: center;
      gap: 14px;
      color: var(--muted);
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      font-size: .82rem;
    }
    .mark {
      width: 42px;
      height: 42px;
      display: grid;
      place-items: center;
      border: 1px solid rgba(40,212,183,.5);
      border-radius: 12px;
      background: rgba(40,212,183,.1);
      color: var(--accent);
      font-weight: 900;
      letter-spacing: 0;
    }
    h1 {
      max-width: 760px;
      margin: 28px 0 16px;
      font-size: clamp(2.4rem, 6vw, 5.4rem);
      line-height: .95;
      letter-spacing: 0;
    }
    .lead {
      max-width: 720px;
      margin: 0;
      color: var(--muted);
      font-size: clamp(1rem, 2vw, 1.18rem);
      line-height: 1.7;
    }
    .status {
      min-width: 220px;
      padding: 16px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: rgba(16,27,46,.78);
    }
    .status strong {
      display: block;
      margin-bottom: 8px;
      color: var(--accent);
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 16px;
      margin-top: 28px;
    }
    section {
      border: 1px solid var(--line);
      border-radius: 8px;
      background: rgba(16,27,46,.82);
      padding: 20px;
    }
    section h2 {
      margin: 0 0 14px;
      font-size: 1rem;
      color: var(--accent-2);
      letter-spacing: 0;
    }
    dl, ul { margin: 0; }
    dl { display: grid; gap: 12px; }
    dt { color: var(--muted); font-size: .84rem; }
    dd { margin: 3px 0 0; font-weight: 700; }
    ul {
      padding-left: 18px;
      color: var(--muted);
      line-height: 1.7;
    }
    .wide { grid-column: span 2; }
    .terminal {
      margin-top: 28px;
      border: 1px solid var(--line);
      border-radius: 8px;
      overflow: hidden;
      background: #050a12;
    }
    .terminal-bar {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 12px 14px;
      background: var(--panel-2);
      color: var(--muted);
      font-size: .82rem;
    }
    .dot { width: 10px; height: 10px; border-radius: 50%; background: var(--accent); }
    pre {
      margin: 0;
      padding: 18px;
      overflow: auto;
      color: #d7fbe8;
      font-size: .92rem;
      line-height: 1.55;
    }
    footer {
      margin-top: 24px;
      color: var(--muted);
      font-size: .9rem;
    }
    @media (max-width: 820px) {
      header { display: block; }
      .status { margin-top: 22px; }
      .grid { grid-template-columns: 1fr; }
      .wide { grid-column: auto; }
    }
  </style>
</head>
<body>
  <main>
    <header>
      <div>
        <div class="brand"><span class="mark">OD</span><span>Olidev style - AlpesNet intranet</span></div>
        <h1>Intranet AlpesNet</h1>
        <p class="lead">Serveur web interne deploye avec Nginx, utilisateur dedie, firewall minimal, logs separes, sauvegarde verifiee et restauration testee.</p>
      </div>
      <aside class="status">
        <strong>HTTP 200 attendu</strong>
        <span>intranet.alpesnet.local</span>
      </aside>
    </header>

    <div class="grid">
      <section>
        <h2>Identite</h2>
        <dl>
          <div><dt>Prenom</dt><dd>Oliv</dd></div>
          <div><dt>Date</dt><dd>2026-06-29</dd></div>
          <div><dt>Serveur</dt><dd>srv-oliv</dd></div>
        </dl>
      </section>

      <section>
        <h2>Securite</h2>
        <ul>
          <li>Worker Nginx sous www-nginx</li>
          <li>SSH restreint au campus</li>
          <li>Fail2ban actif sur sshd</li>
          <li>Port 80 ouvert pour l'intranet</li>
        </ul>
      </section>

      <section>
        <h2>Exploitation</h2>
        <ul>
          <li>Vhost dedie</li>
          <li>Logs separes</li>
          <li>Rotation 7 jours compressee</li>
          <li>Checksum SHA-256</li>
        </ul>
      </section>

      <section class="wide">
        <h2>Livrables RNCP</h2>
        <ul>
          <li>Configuration Nginx annotee dans /etc/nginx/sites-available/intranet</li>
          <li>Sauvegarde de /var/www dans /backup/www</li>
          <li>Restauration testee dans /tmp/restauration-www-test</li>
          <li>Rapport automatique dans /var/log/alpesnet-it5</li>
        </ul>
      </section>

      <section>
        <h2>Validation</h2>
        <dl>
          <div><dt>Commande</dt><dd>curl -v http://intranet.alpesnet.local</dd></div>
          <div><dt>Resultat</dt><dd>Page servie par Nginx</dd></div>
        </dl>
      </section>
    </div>

    <div class="terminal">
      <div class="terminal-bar"><span class="dot"></span><span>controle-alpesnet.sh</span></div>
      <pre>nginx -t
ps -eo user,args | grep "[n]ginx"
sudo ufw status verbose
sha256sum -c /backup/www-checksums.txt</pre>
    </div>

    <footer>AlpesNet - deploiement automatise par Oliv - 2026-06-29</footer>
  </main>
</body>
</html>
HTML
chown -R root:'www-nginx' '/var/www/intranet.alpesnet.local'; chmod -R 750 '/var/www/intranet.alpesnet.local'; find '/var/www/intranet.alpesnet.local' -maxdepth 3 -ls
```

**Resultat** : code retour `0`

```text
   526674      4 drwxr-x---   3 root     www-nginx     4096 juin 26 14:06 /var/www/intranet.alpesnet.local
   526682      4 drwxr-x---   2 root     www-nginx     4096 juin 26 14:06 /var/www/intranet.alpesnet.local/html
   526688      8 -rwxr-x---   1 root     www-nginx     6226 juin 29 09:46 /var/www/intranet.alpesnet.local/html/index.html
```

**Explication**

Cree une page intranet plus professionnelle, inspiree d'une page portfolio/dev sobre, avec prenom, date, nom du serveur, statut, securite, livrables et commandes de verification.

## Configurer le vhost intranet

**Commande**

```bash
printf '%s\n' 'server {' '    listen 80;' '    server_name intranet.alpesnet.local;' '' '    root /var/www/intranet.alpesnet.local/html;' '    index index.html;' '' '    access_log /var/log/nginx/intranet_access.log;' '    error_log /var/log/nginx/intranet_error.log warn;' '' '    location / {' '        try_files $uri $uri/ =404;' '    }' '}' > /etc/nginx/sites-available/intranet; ln -sfn /etc/nginx/sites-available/intranet /etc/nginx/sites-enabled/intranet; rm -f /etc/nginx/sites-enabled/default; nginx -t
```

**Resultat** : code retour `0`

```text
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

**Explication**

Declare le vhost intranet.alpesnet.local, la racine web, les logs separes et desactive le site par defaut.

## Ajouter la resolution locale du nom intranet

**Commande**

```bash
grep -qE '[[:space:]]intranet.alpesnet.local( |$)' /etc/hosts || printf '%s\n' '127.0.0.1 intranet.alpesnet.local' >> /etc/hosts; grep 'intranet.alpesnet.local' /etc/hosts
```

**Resultat** : code retour `0`

```text
127.0.0.1 intranet.alpesnet.local
```

**Explication**

Permet de tester curl http://intranet.alpesnet.local directement depuis la VM sans DNS externe.

## Demarrer et verifier Nginx

**Commande**

```bash
systemctl enable --now nginx; systemctl reload nginx; systemctl --no-pager --full status nginx | sed -n '1,14p'
```

**Resultat** : code retour `0`

```text
Synchronizing state of nginx.service with SysV service script with /lib/systemd/systemd-sysv-install.
Executing: /lib/systemd/systemd-sysv-install enable nginx
● nginx.service - A high performance web server and a reverse proxy server
     Loaded: loaded (/lib/systemd/system/nginx.service; enabled; preset: enabled)
     Active: active (running) since Mon 2026-06-29 08:51:09 CEST; 55min ago
       Docs: man:nginx(8)
    Process: 3519 ExecReload=/usr/sbin/nginx -g daemon on; master_process on; -s reload (code=exited, status=0/SUCCESS)
   Main PID: 670 (nginx)
      Tasks: 5 (limit: 2298)
     Memory: 7.1M
        CPU: 16ms
     CGroup: /system.slice/nginx.service
             ├─ 670 "nginx: master process /usr/sbin/nginx -g daemon on; master_process on;"
             ├─1518 "nginx: worker process"
             ├─1519 "nginx: worker process"
             ├─3520 "nginx: worker process"
```

**Explication**

Demarre Nginx, applique la configuration et garde une preuve de service actif.


# Autonomie 3 - Firewall et Fail2ban

## Configurer UFW pour SSH restreint et HTTP ouvert

**Commande**

```bash
ufw default deny incoming; ufw default allow outgoing; ufw allow from '192.168.56.0/24' to any port 22 proto tcp; ufw allow 80/tcp; ufw --force enable; ufw status numbered
```

**Resultat** : code retour `0`

```text
Default incoming policy changed to 'deny'
(be sure to update your rules accordingly)
Default outgoing policy changed to 'allow'
(be sure to update your rules accordingly)
Skipping adding existing rule
Skipping adding existing rule
Skipping adding existing rule (v6)
Firewall is active and enabled on system startup
Status: active

     To                         Action      From
     --                         ------      ----
[ 1] 2049                       ALLOW IN    192.168.0.0/24            
[ 2] Samba                      ALLOW IN    Anywhere                  
[ 3] 22                         ALLOW IN    192.168.0.0/24            
[ 4] 22                         ALLOW IN    192.168.56.0/24           
[ 5] 22/tcp                     ALLOW IN    192.168.56.0/24           
[ 6] 2049                       ALLOW IN    192.168.56.0/24           
[ 7] 445/tcp                    ALLOW IN    192.168.56.0/24           
[ 8] 139/tcp                    ALLOW IN    192.168.56.0/24           
[ 9] 80/tcp                     ALLOW IN    Anywhere                  
[10] Samba (v6)                 ALLOW IN    Anywhere (v6)             
[11] 80/tcp (v6)                ALLOW IN    Anywhere (v6)             

```

**Explication**

Applique le filtrage demande : SSH limite au sous-reseau campus et HTTP ouvert pour l'intranet.

## Verifier Fail2ban SSH

**Commande**

```bash
mkdir -p /etc/fail2ban/jail.d; printf '%s\n' '[sshd]' 'enabled = true' > /etc/fail2ban/jail.d/alpesnet-sshd.local; fail2ban-client -t && systemctl enable --now fail2ban && systemctl restart fail2ban && sleep 1 && fail2ban-client status sshd
```

**Resultat** : code retour `0`

```text
2026-06-29 09:46:42,971 fail2ban.configreader   [4559]: WARNING 'allowipv6' not defined in 'Definition'. Using default one: 'auto'
OK: configuration test is successful
Synchronizing state of fail2ban.service with SysV service script with /lib/systemd/systemd-sysv-install.
Executing: /lib/systemd/systemd-sysv-install enable fail2ban
Status for the jail: sshd
|- Filter
|  |- Currently failed:	0
|  |- Total failed:	0
|  `- File list:	/var/log/auth.log
`- Actions
   |- Currently banned:	0
   |- Total banned:	0
   `- Banned IP list:	
```

**Explication**

Active et verifie la jail SSH Fail2ban pour proteger le serveur contre les tentatives de force brute.


# Autonomie 3 - Rotation et sauvegarde web

## Configurer logrotate pour les logs intranet

**Commande**

```bash
printf '%s\n' '/var/log/nginx/intranet_access.log /var/log/nginx/intranet_error.log {' '    daily' '    rotate 7' '    compress' '    missingok' '    notifempty' '    create 0640 www-nginx adm' '    sharedscripts' '    postrotate' '        [ -s /run/nginx.pid ] && kill -USR1 $(cat /run/nginx.pid)' '    endscript' '}' > /etc/logrotate.d/nginx-intranet; logrotate -d /etc/logrotate.d/nginx-intranet
```

**Resultat** : code retour `0`

```text
warning: logrotate in debug mode does nothing except printing debug messages!  Consider using verbose mode (-v) instead if this is not what you want.

reading config file /etc/logrotate.d/nginx-intranet
Reading state from file: /var/lib/logrotate/status
Allocating hash table for state file, size 64 entries
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state
Creating new state

Handling 1 logs

rotating pattern: /var/log/nginx/intranet_access.log /var/log/nginx/intranet_error.log  after 1 days (7 rotations)
empty log files are not rotated, old logs are removed
considering log /var/log/nginx/intranet_access.log
  Now: 2026-06-29 09:46
  Last rotated at 2026-06-29 08:00
  log does not need rotating (log has been rotated at 2026-06-29 08:00, which is less than a day ago)
considering log /var/log/nginx/intranet_error.log
  Now: 2026-06-29 09:46
  Last rotated at 2026-06-29 08:00
  log does not need rotating (log has been rotated at 2026-06-29 08:00, which is less than a day ago)
not running postrotate script, since no logs were rotated
```

**Explication**

Configure une rotation quotidienne sur 7 jours avec compression pour les logs separes du vhost.

## Sauvegarder /var/www dans /backup/www

**Commande**

```bash
mkdir -p '/backup/www'; rsync -avz --delete /var/www/ '/backup/www/'; find '/backup/www' -maxdepth 4 -type f -ls
```

**Resultat** : code retour `0`

```text
sending incremental file list
intranet.alpesnet.local/html/index.html

sent 2.681 bytes  received 39 bytes  5.440,00 bytes/sec
total size is 6.851  speedup is 2,52
   131106      4 -rw-r--r--   1 root     root          615 juin 22 10:40 /backup/www/html/index.nginx-debian.html
   131110      8 -rwxr-x---   1 root     www-nginx     6226 juin 29 09:46 /backup/www/intranet.alpesnet.local/html/index.html
   131108      4 -rw-r--r--   1 root     root            10 juin 23 14:30 /backup/www/upload/index.php
```

**Explication**

Sauvegarde le contenu web, dont la page intranet, dans /backup/www.

## Generer et verifier le checksum de la sauvegarde web

**Commande**

```bash
find '/backup/www' -type f -print0 | sort -z | xargs -0 sha256sum > '/backup/www-checksums.txt'; cd / && sha256sum -c '/backup/www-checksums.txt'
```

**Resultat** : code retour `0`

```text
/backup/www/html/index.nginx-debian.html: Réussi
/backup/www/intranet.alpesnet.local/html/index.html: Réussi
/backup/www/upload/index.php: Réussi
```

**Explication**

Produit et verifie les empreintes SHA-256 des fichiers sauvegardes dans /backup/www.

## Tester la restauration de /var/www

**Commande**

```bash
rm -rf '/tmp/restauration-www-test'; mkdir -p '/tmp/restauration-www-test'; rsync -avz '/backup/www/' '/tmp/restauration-www-test/'; test -f '/tmp/restauration-www-test/intranet.alpesnet.local/html/index.html'; sed -n '1,20p' '/tmp/restauration-www-test/intranet.alpesnet.local/html/index.html'
```

**Resultat** : code retour `0`

```text
sending incremental file list
./
html/
html/index.nginx-debian.html
intranet.alpesnet.local/
intranet.alpesnet.local/html/
intranet.alpesnet.local/html/index.html
upload/
upload/index.php

sent 3.113 bytes  received 96 bytes  6.418,00 bytes/sec
total size is 6.851  speedup is 2,13
<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Intranet AlpesNet - Oliv</title>
  <style>
    :root {
      color-scheme: dark;
      --bg: #09111f;
      --panel: #101b2e;
      --panel-2: #142238;
      --text: #eef6ff;
      --muted: #9fb2c8;
      --line: rgba(255,255,255,.12);
      --accent: #28d4b7;
      --accent-2: #5aa9ff;
      --warn: #ffd166;
    }
    * { box-sizing: border-box; }
```

**Explication**

Restaure la sauvegarde web dans un repertoire de test et verifie que index.html est lisible.


# Autonomie 3 - Criteres de reussite

## Verifier HTTP 200

**Commande**

```bash
code=$(curl -s -o /tmp/intranet-curl-body.txt -w '%{http_code}' 'http://intranet.alpesnet.local'); printf '%s\n' "$code"; test "$code" = '200'
```

**Resultat** : code retour `0`

```text
200
```

**Explication**

Verifie que le vhost intranet repond exactement en HTTP 200. Si le code est different, le script echoue.

## Verifier le worker Nginx sous www-nginx

**Commande**

```bash
ps -eo user:32,args | awk '$1 == "'www-nginx'" && /nginx: worker process/ {found=1; print} END {exit !found}'
```

**Resultat** : code retour `0`

```text
www-nginx                        nginx: worker process
www-nginx                        nginx: worker process
```

**Explication**

Prouve que les workers Nginx tournent sous l'utilisateur dedie et pas sous root.

## Verifier les regles UFW finales

**Commande**

```bash
ufw status verbose
```

**Resultat** : code retour `0`

```text
Status: active
Logging: on (low)
Default: deny (incoming), allow (outgoing), disabled (routed)
New profiles: skip

To                         Action      From
--                         ------      ----
2049                       ALLOW IN    192.168.0.0/24            
137,138/udp (Samba)        ALLOW IN    Anywhere                  
139,445/tcp (Samba)        ALLOW IN    Anywhere                  
22                         ALLOW IN    192.168.0.0/24            
22                         ALLOW IN    192.168.56.0/24           
22/tcp                     ALLOW IN    192.168.56.0/24           
2049                       ALLOW IN    192.168.56.0/24           
445/tcp                    ALLOW IN    192.168.56.0/24           
139/tcp                    ALLOW IN    192.168.56.0/24           
80/tcp                     ALLOW IN    Anywhere                  
137,138/udp (Samba (v6))   ALLOW IN    Anywhere (v6)             
139,445/tcp (Samba (v6))   ALLOW IN    Anywhere (v6)             
80/tcp (v6)                ALLOW IN    Anywhere (v6)             

```

**Explication**

Conserve la preuve des ports exposes : SSH restreint au sous-reseau et HTTP 80 ouvert.

## Verifier les checksums finaux

**Commande**

```bash
cd / && sha256sum -c '/backup/checksums.txt' && sha256sum -c '/backup/www-checksums.txt'
```

**Resultat** : code retour `0`

```text
/backup/configs-20260629.tar.gz: Réussi
/backup/www/html/index.nginx-debian.html: Réussi
/backup/www/intranet.alpesnet.local/html/index.html: Réussi
/backup/www/upload/index.php: Réussi
```

**Explication**

Valide les checksums des configurations et de la sauvegarde web.

# Lecture rapide pour jury

## Timeline d'execution

| Etape | Action | Code retour |
| --- | --- | --- |
| 01 | Preparer le dossier de sauvegarde | `0` |
| 02 | Verifier ou creer la source AlpesNet | `0` |
| 03 | Executer la sauvegarde rsync | `0` |
| 04 | Verifier le contenu sauvegarde | `0` |
| 05 | Verifier rsync en dry-run checksum | `0` |
| 06 | Creer l'archive des configurations | `0` |
| 07 | Lister le contenu de l'archive | `0` |
| 08 | Tester la restauration dans un repertoire temporaire | `0` |
| 09 | Generer et verifier le checksum | `0` |
| 10 | Generer la procedure de restauration SSH | `0` |
| 11 | Installer Nginx et les outils de securite | `0` |
| 12 | Creer l'utilisateur dedie Nginx | `0` |
| 13 | Configurer la directive user de Nginx | `0` |
| 14 | Creer la racine web et la page intranet | `0` |
| 15 | Configurer le vhost intranet | `0` |
| 16 | Ajouter la resolution locale du nom intranet | `0` |
| 17 | Demarrer et verifier Nginx | `0` |
| 18 | Configurer UFW pour SSH restreint et HTTP ouvert | `0` |
| 19 | Verifier Fail2ban SSH | `0` |
| 20 | Configurer logrotate pour les logs intranet | `0` |
| 21 | Sauvegarder /var/www dans /backup/www | `0` |
| 22 | Generer et verifier le checksum de la sauvegarde web | `0` |
| 23 | Tester la restauration de /var/www | `0` |
| 24 | Verifier HTTP 200 | `0` |
| 25 | Verifier le worker Nginx sous www-nginx | `0` |
| 26 | Verifier les regles UFW finales | `0` |
| 27 | Verifier les checksums finaux | `0` |

## Causes et risques traites

| Cause ou risque initial | Effet possible | Reponse appliquee |
| --- | --- | --- |
| Sauvegarde non testee | Impossible de prouver une reprise apres incident | Sauvegarde rsync, archive tar, restauration dans /tmp et checksum |
| Integrite non controlee | Archive modifiee ou corrompue non detectee | Empreintes SHA-256 et verification avec sha256sum -c |
| Serveur web trop generique | Confusion avec le site par defaut ou logs melanges | Vhost intranet dedie, site default desactive, logs separes |
| Processus web trop privilegie | Impact plus fort en cas de compromission | Worker Nginx execute sous www-nginx |
| Exposition SSH trop large | Surface d'attaque plus grande | UFW avec SSH restreint au sous-reseau 192.168.56.0/24 |
| Tentatives SSH repetees | Risque de force brute | Fail2ban actif sur la jail sshd |

## Actions correctives et commandes de verification

| Action corrective | Commande appliquee | Verification associee |
| --- | --- | --- |
| Creer la sauvegarde des donnees | `rsync -avz --delete '/srv/alpesnet/' '/backup/alpesnet-20260629/'` | `find '/backup/alpesnet-20260629' -maxdepth 2 -type f -ls` et `rsync --dry-run --checksum` |
| Archiver les configurations | `tar -czf '/backup/configs-20260629.tar.gz' /etc/ssh /etc/rsyslog.d` | `tar -tzf '/backup/configs-20260629.tar.gz'` |
| Tester la restauration configs | `tar -xzf '/backup/configs-20260629.tar.gz' -C '/tmp/restauration-test'` | `ls '/tmp/restauration-test/etc/ssh'` et lecture de `sshd_config` |
| Verifier l'integrite configs | `sha256sum '/backup/configs-20260629.tar.gz' > '/backup/checksums.txt'` | `sha256sum -c '/backup/checksums.txt'` |
| Durcir l'execution Nginx | directive `user www-nginx;` dans `/etc/nginx/nginx.conf` | `ps -eo user,args | grep '[n]ginx'` |
| Publier l'intranet | vhost `/etc/nginx/sites-available/intranet` | `nginx -t` puis `curl http://intranet.alpesnet.local` |
| Filtrer le reseau | UFW SSH restreint + HTTP ouvert | `ufw status verbose` |
| Activer l'anti force brute | jail Fail2ban `sshd` | `fail2ban-client status sshd` |
| Sauvegarder /var/www | `rsync -avz --delete /var/www/ '/backup/www/'` | `sha256sum -c '/backup/www-checksums.txt'` et restauration testee |

## Etat final verifiable

| Controle | Commande | Etat attendu |
| --- | --- | --- |
| Intranet HTTP | `curl -s -o /tmp/intranet-curl-body.txt -w '%{http_code}' http://intranet.alpesnet.local` | `200` |
| Worker dedie | `ps -eo user,args | grep '[n]ginx: worker'` | `www-nginx` |
| Pare-feu | `ufw status verbose` | SSH restreint au campus, HTTP 80 ouvert |
| Sauvegardes | `sha256sum -c '/backup/checksums.txt'` et `sha256sum -c '/backup/www-checksums.txt'` | `OK` |
| Restauration | `test -f '/tmp/restauration-www-test/intranet.alpesnet.local/html/index.html'` | fichier restauré lisible |

# Synthese finale

- Rapport Markdown : `/var/log/alpesnet-it5/rapport-it5-20260629_094637.md`
- Log brut : `/var/log/alpesnet-it5/execution-it5-20260629_094637.log`
- Sauvegarde rsync : `/backup/alpesnet-20260629`
- Archive configs : `/backup/configs-20260629.tar.gz`
- Checksum : `/backup/checksums.txt`
- Restauration testee : `/tmp/restauration-test`
- Vhost Nginx : `intranet.alpesnet.local`
- Racine intranet : `/var/www/intranet.alpesnet.local/html`
- Sauvegarde web : `/backup/www`
- Restauration web testee : `/tmp/restauration-www-test`

Conclusion : la sauvegarde est creee, verifiee, restauree dans un repertoire de test, puis le serveur Nginx intranet est deployee et controle selon les criteres Autonomie 3.
